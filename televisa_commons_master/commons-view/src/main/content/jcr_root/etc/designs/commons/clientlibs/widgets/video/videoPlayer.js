
function videoPlayerSelectionChanged(t, value, isChecked){
   /*TODO implement tab switching functionality*/
   /* 
    var parent = t.findParentByType("panel");
    var youtubeContainer = parent.getComponent("youtube");
    var brightcoveContainer = parent.getComponent("brightcove");
    var akamaiContainer = parent.getComponent("akamai");
   
    if(value == "youtube"){
        youtubeContainer.expand();
        brightcoveContainer.collapse();
        akamaiContainer.collapse();
    }else if(value == "brightcove"){
        brightcoveContainer.expand();
        youtubeContainer.collapse();
        akamaiContainer.collapse();
    }else if(value == "akamai"){
        akamaiContainer.expand();
        brightcoveContainer.collapse();
        youtubeContainer.collapse();
    }*/
   //console.log("selection");
   //debugger;
   //t.findParentByType('tabpanel').findById('youtube').hide();
   //t.findParentByType("dialog").getComponent(0).getComponent("youtube").hide();
   //t.findParentByType("dialog").getComponent(0).getComponent("youtube").setVisible(false);
    
} 

function videoPlayerLoadContent(field,record,path){
    /*TODO implement tab hiding functionality. */
   /*
    var value = field.getValue();
    var parent = field.findParentByType("panel");
    var youtubeContainer = parent.getComponent("youtube");
    var brightcoveContainer = parent.getComponent("brightcove");
    var akamaiContainer = parent.getComponent("akamai");
    
    if(value == "youtube"){
        youtubeContainer.expand();
        brightcoveContainer.collapse();
        akamaiContainer.collapse();
    }else if(value == "brightcove"){
        brightcoveContainer.expand();
        youtubeContainer.collapse();
        akamaiContainer.collapse();
    }else if(value == "akamai"){
        akamaiContainer.expand();
        brightcoveContainer.collapse();
        youtubeContainer.collapse();
    }  
    */   
   
} 